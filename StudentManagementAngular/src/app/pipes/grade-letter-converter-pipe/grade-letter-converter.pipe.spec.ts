import { GradeLetterConverterPipe } from './grade-letter-converter.pipe';

describe('GradeLetterConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new GradeLetterConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
