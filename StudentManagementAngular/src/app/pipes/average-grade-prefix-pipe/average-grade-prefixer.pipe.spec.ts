import { AverageGradePrefixerPipe } from './average-grade-prefixer.pipe';

describe('AverageGradePrefixerPipe', () => {
  it('create an instance', () => {
    const pipe = new AverageGradePrefixerPipe();
    expect(pipe).toBeTruthy();
  });
});
